import { Home, ArrowLeftRight, History, CreditCard, Loader2 } from "lucide-react"

export const Icons = {
  home: Home,
  transfer: ArrowLeftRight,
  history: History,
  creditCard: CreditCard,
  spinner: Loader2,
}